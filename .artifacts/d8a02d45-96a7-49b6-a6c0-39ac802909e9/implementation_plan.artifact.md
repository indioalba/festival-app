# Fix CI Failure: Local tests and APKs

The GitHub Action "Local tests and APKs" is failing at the Roborazzi screenshot verification step. This task aims to investigate the cause of the failure and align the CI workflow with the project rules regarding screenshot generation.

## User Review Required

> [!IMPORTANT]
> The project rules state: *"Screenshot tests are generated by CI, so they shouldn't be checked into the repo from a workstation."*
>
> Currently, the CI runs `verifyRoborazziDemoDebug`, which expects baseline screenshots to exist. Since these are not in the repository, the task fails. We need to decide how the CI should "generate" and "verify" these screenshots.

## Proposed Changes

### CI Workflow
#### [MODIFY] [Build.yaml](file:///Users/indioalba/Workspace/Festival/.github/workflows/Build.yaml)
- Add a step to upload Roborazzi outputs (screenshots and reports) as artifacts, especially on failure. This will allow for inspection of what the CI is generating.
- Investigate if the `verifyRoborazzi` step should be preceded by a `record` step or if we should use a different mechanism to handle the "CI-generated" screenshots.

## Verification Plan

### Automated Tests
- The primary verification will be the successful completion of the GitHub Action after the changes are pushed.
- Locally, verify that the `Build.yaml` changes are syntactically correct.

### Manual Verification
- Inspect the uploaded artifacts on GitHub to ensure they contain the expected screenshots.
